# Compacted History Handoff Smoke

Execute the compiled goal graph exactly. The fixture is intentionally limited to
one predecessor marker and one dependent confirmation so a serial Harness run
can prove the V2 compacted-history boundary without external workspace setup.

Use only `/workspace` and finish every assigned goal with `end_goal`.
