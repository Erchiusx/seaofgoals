You are testing the test-with-postgres skill.

Task:
Run the PostgreSQL-backed Go tests in /workspace.

If end_goal is available, call it once when the assigned goal is complete, with its id, status, and summary.
Use shell for local inspection and test commands.
Use record_effect for observed database effects, test result artifacts, and produced traceable outputs.

Important harness constraint:
The PostgreSQL dependency is already supplied by Docker Compose at $POSTGRES_TEST_DB.
Do not try to run Docker inside the runner container.
Use the supplied connection string when invoking go test.

Expected command shape:
go test -v ./... -postgres-test-db "$POSTGRES_TEST_DB"

Do not call external business APIs.
Do not finish until the tests have run and the result has been recorded.
