# database-migrations Experiment

Runs the `database-migrations` skill against a small PostgreSQL migration fixture.

Run from the repository root:

```bash
make build-workflows
make experiment-database-migrations
make view-trace SKILL_EXPERIMENT=database-migrations
```
