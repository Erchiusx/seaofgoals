# nextjs-performance Experiment

Runs the `nextjs-performance` skill against a small local Next.js-like fixture.

Run from the repository root:

```bash
make build-workflows
make experiment-nextjs-performance
make view-trace SKILL_EXPERIMENT=nextjs-performance
```
