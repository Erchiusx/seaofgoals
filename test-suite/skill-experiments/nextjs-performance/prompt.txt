You are testing the nextjs-performance skill.

Task:
Review and fix the small Next.js-like fixture in /workspace so it satisfies the performance quick checks from the skill.

If end_goal is available, call it once when the assigned goal is complete, with its id, status, and summary.
Use shell for local inspection commands.
Use write_file for actual file edits, writing the complete target file content.
Use record_effect after a file edit, validation command, or produced artifact.
Do not finish until all concrete fixture issues below have been handled and inspected.
When searching the workspace, exclude sog-trace.jsonl and other generated trace/log files.

Concrete fixture issues:
- app/dashboard/page.tsx uses sequential awaits for independent fetches and has no Suspense boundary.
- components/Toolbar.tsx imports all of lucide-react instead of direct icon modules.
- components/ChartPanel.tsx eagerly imports a heavy chart implementation instead of using next/dynamic.
- app/actions/updateProject.ts lacks an in-action auth check and ownership validation.
- next.config.js does not enable standalone output for Docker production.

Validate with:
node scripts/check-performance.js

Do not call external business APIs.
