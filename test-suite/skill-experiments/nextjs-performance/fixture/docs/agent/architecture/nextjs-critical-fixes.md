# Next.js Critical Fixes

- Fetch independent data with `Promise.all`.
- Wrap slow dashboard data behind a Suspense boundary.
- Import lucide icons from direct module paths.
- Use `next/dynamic` for heavy chart components.
- Validate auth and ownership inside Server Actions.
- Use `output: "standalone"` for production Docker images.
