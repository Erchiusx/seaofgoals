spring:
  datasource:
    url: jdbc:mysql://localhost:3306/demo
    username: demo
    password: demo
    validation-query: SELECT 1 FROM DUAL

mybatis-plus:
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

logging:
  level:
    com.example.demo.dal.mysql: debug
