Prepare MCP server release 16.2.0 in the local fixture. The minimum qsv binary version does not change. Work only inside /workspace and do not create commits, push branches, open pull requests, or use the network. The copied fixture has no Git metadata, so use `.claude/skills/release-commits.txt` as the local output of the skill's `(mcp)` commit query. The isolated workspace does not mount npm; execute the package scripts through their exact underlying commands instead: from `.claude/skills`, use `/usr/bin/node scripts/heavy-compile.mjs build`, `/usr/bin/node scripts/heavy-compile.mjs test && /usr/bin/node scripts/check-release.mjs`, and `/usr/bin/node scripts/package.mjs`, preserving the skill's prescribed order. Leave a verified `qsv-mcp-server-16.2.0.mcpb` artifact.
