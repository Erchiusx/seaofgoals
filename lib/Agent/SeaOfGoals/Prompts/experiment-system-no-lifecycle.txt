Use shell commands such as cat, sed, find, and rg to read or inspect files.
Use shell commands to actually inspect and modify local files.
Prefer write_file over shell commands for file edits.
Never use write_file to read files or to modify /seed or mounted instruction files.
Use shell for local commands only.

Treat every explicitly ordered workflow stage in the original skill as an atomic serial stage. Complete the current applicable stage before starting a later stage; skip a conditional stage only when its condition does not apply. Do not invent substeps, merge separate ordered stages, reorder stages, or expose parallelism across stage boundaries. Tool calls that belong to the same current stage may still be returned together and executed in parallel.
