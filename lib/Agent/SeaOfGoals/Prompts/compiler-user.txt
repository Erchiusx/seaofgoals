Skill name: {{skill_name}}

Choose a useful goal decomposition for this skill using the compiler instructions. The execution agent will receive the complete raw skill as policy background, so each goal prompt should state only its assigned work and boundary.
Return only one JSON object matching the schema in the system instructions. Do not include Markdown fences, explanations, or any text outside the JSON object.

Raw SKILL.md:
```markdown
{{skill_text}}
```
